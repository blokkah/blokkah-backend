const express = require('express');
// const validate = require('../../middlewares/validate');
// const studentController = require('../../modules/student/student.controller');
// const studentValidation = require('../../modules/student/student.validation');

const router = express.Router();

// router.post('/', validate(studentValidation.createStudent), studentController.createStudent);
module.exports = router;
